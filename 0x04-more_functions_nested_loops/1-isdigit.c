#include "main.h"

/**
 * _isdigit - Checks for a digit (0 through 9).
 * @c: will be checked if it is a digit  or not
 *Return: Always (1)
 */
int _isdigit(int c)
{
if (c == 0 && c <= 9)
{
return (1);
}
else
{
return (0);
}
return (0);
}
