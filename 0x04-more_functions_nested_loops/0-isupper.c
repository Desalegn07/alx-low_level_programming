#include "main.h"

/**
 * _isupper - Checks for uppercase character.
 * @c: Will be checked for being uppercase or lowercase
 * Return: Always 1 (success)
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
return (0);
}
