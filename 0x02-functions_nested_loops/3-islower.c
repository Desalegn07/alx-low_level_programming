#include "main.h"
/**
* _islower.c - Check if char is lowercase.
* @c: is the char to be checked
*Return: 1 if the char is lowercase, othereise 0.
*/
int_islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
returni(0);
}
